import type { FeatureModule } from "./types";

/**
 * Opportunity Card Favourites
 *
 * On hover over a .hand__card element, shows an overlay allowing the
 * player to mark the card as a favourite or one to avoid.
 */
export const cardFavourites: FeatureModule = {
  id: "card-favourites",

  enable() {
    // TODO: Phase 3 implementation
    console.log("[Fallen Tweaks] card-favourites: ready for implementation");
  },

  disable() {
    // TODO: Phase 3 cleanup
  },
};
